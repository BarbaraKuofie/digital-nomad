<!DOCTYPE html>
<html>
<head>

  <title class="sign-up"> sign up </title>

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/normalize/5.0.0/normalize.min.css" />

</head>

<body class="sign-up">
<!-- WEB PAGE CONTENT BELOW-->
<section class="sign-up">
    <h2 class="title">Join The Ditigal Nomad Community!</h2>

    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

    <div class="field">
        <%= f.label :name, :class=>"formstyle"  %><br />
        <%= f.text_field :name, autofocus: true, autocomplete: "name"%><br />
      </div>
        <%= f.label 'Host or Nomad?', :class=>"formstyle"%><br />
        <%= f.select :host, [["Host", true], ['Nomad', false]] %> <br />
      </div>

    <div>
      <% if @user.avatar.attached? %>
      <%= image_tag @user.avatar, style: "width: 100px; display: block"%>
      <%end%>
      </div>
  <div>
        <%= f.label "Profile Picture" , :class=>"formstyle" %><br />
       <%= f.file_field :avatar, :class=>"right-shift "%> <br />
      </div>

      <div class="field">
        <%= f.label :email , :class=>"formstyle"  %><br />
        <%= f.email_field :email, autocomplete: "email" %>
      </div>

      <div class="field">
        <%= f.label :password , :class=>"formstyle" %>
        <% if @minimum_password_length %>
        <em>(<%= @minimum_password_length %> characters minimum)</em>
        <% end %><br />
        <%= f.password_field :password, autocomplete: "new-password" %>
      </div>

      <div >
        <%= f.label :password_confirmation , :class=>"formstyle"  %><br />
        <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
      </div>

      <div class="button">
      <%= f.submit "Sign up", :class=>"btn btn-info"%><br><br>
      </div>
    <% end %>
<section>
<%= render "devise/shared/links" %>
</body>
</html>